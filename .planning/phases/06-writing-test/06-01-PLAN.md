phase: 06-writing-test
plan: 01
type: execute
wave: 1
depends_on:

- 05-PLAN-microservices
  files_modified:
- frontend/src/pages/WritingTestPage.jsx
- frontend/src/api/interview.js
- backend/controllers/writingController.js
- backend/routes/writing.js
- backend/models/Session.js
- ai-services/nlp-service/main.py
  autonomous: true
  requirements:
- REQ-writing-test
  must_haves:
  truths: - "Users can start a timed writing test from the dashboard and submit text before the 10-minute timer expires" - "Server accepts and persists writing submissions to the Session model" - "Writing submissions are analyzed by the NLP microservice and results are stored on the session (writingAnalysis)"
  artifacts: - path: "frontend/src/pages/WritingTestPage.jsx"
  provides: "UI for writing test with 10-minute timer and submission button" - path: "backend/controllers/writingController.js"
  provides: "POST /api/sessions/:sessionId/writing to accept text submissions and update session" - path: "ai-services/nlp-service/main.py"
  provides: "/analyze endpoint returning nlpAnalysisSchema"
  key_links: - from: "frontend/src/pages/WritingTestPage.jsx"
  to: "/api/sessions/:sessionId/writing"
  via: "fetch POST with JSON { text }" - from: "backend/controllers/writingController.js"
  to: "ai-services/nlp-service"
  via: "axios POST /analyze with body { text }"

<objective>
Implement the timed written-evaluation flow (10-minute test), server persistence, and NLP analysis integration.

Purpose: allow the platform to evaluate written answers using the same analysis pipeline and store results for reports.
Output: frontend page, backend API, and NLP microservice stub.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-ai-analysis/05-CONTEXT.md
@backend/models/Session.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frontend — WritingTestPage</name>
  <files>frontend/src/pages/WritingTestPage.jsx, frontend/src/api/interview.js</files>
  <action>
    - Create `frontend/src/pages/WritingTestPage.jsx` implementing a 10-minute countdown timer, a large textarea for the user to type their answer, a live character/word counter, and a `Submit` button.
    - On mount, verify session exists via `sessionsApi.get(sessionId)`; if missing navigate back to dashboard.
    - On submit, call `POST /api/sessions/:sessionId/writing` (JSON body: `{ text }`) via `interviewApi` or `api` client, then navigate to `/interview/processing`.
    - Display client-side validation: non-empty, max 5000 characters.
    - Wire the page into `frontend/src/App.jsx` routes.
  </action>
  <verify>
    <automated>node -e "process.exit(require('fs').existsSync('frontend/src/pages/WritingTestPage.jsx')?0:2)"</automated>
  </verify>
  <done>Users can open the page, complete the 10-minute test, and submit text which triggers the backend API.</done>
</task>

<task type="auto">
  <name>Task 2: Backend — Writing API and persistence</name>
  <files>backend/controllers/writingController.js, backend/routes/writing.js, backend/models/Session.js</files>
  <action>
    - Create `backend/controllers/writingController.js` with `submitWriting(req, res)`:
      - Validate `req.body.text` (non-empty, <=5000 chars).
      - Find session by `req.params.sessionId` and ownership (`req.user._id`).
      - Save `session.writingSubmission = text`, set `session.status='processing'`, set `session.jobStatus='queued'` and persist.
      - Trigger analysis pipeline (enqueue job or call analysis service) for writing: use existing `startAnalysis(sessionId)` or add a dedicated `startWritingAnalysis(sessionId)` that enqueues the job.
      - Return 202 Accepted with session summary.
    - Add `backend/routes/writing.js` mounted under `/api/sessions` with `POST /:sessionId/writing` protected by `protect` middleware.
  </action>
  <verify>
    <automated>node -e "process.exit(require('fs').existsSync('backend/controllers/writingController.js')?0:2)"</automated>
  </verify>
  <done>Server accepts writing submissions, persists them on the Session, and enqueues analysis work.</done>
</task>

<task type="auto">
  <name>Task 3: NLP microservice stub & analysis wiring</name>
  <files>ai-services/nlp-service/main.py, backend/services/analysisService.js</files>
  <action>
    - Create `ai-services/nlp-service/main.py` FastAPI stub with `/analyze` that accepts `text: str` and returns an `nlpAnalysisSchema` matching `backend/models/Session.js` (`relevanceScore`, `structureScore`, `grammarScore`, `completenessScore`, `feedback`). Include a simulated delay (2–5s) and placeholder scores.
    - Update `backend/services/analysisService.js` worker to, for any `session.writingSubmission`, call `NLP_SERVICE_URL/analyze` with `{ text }` and persist `answer.writingAnalysis` or `session.writingAnalysis` accordingly.
    - Ensure env var `NLP_SERVICE_URL` is read with a sensible default (`http://127.0.0.1:8003`).
  </action>
  <verify>
    <automated>node -e "process.exit(require('fs').existsSync('ai-services/nlp-service/main.py')?0:2)"</automated>
  </verify>
  <done>NLP service stub exists and the worker saves `writingAnalysis` to the Session after analysis.</done>
</task>

</tasks>

<verification>
- Run basic smoke checks: page file exists, controller exists, and microservice stub exists.
- Integration verification (manual or CI): Submit a writing test and observe session.status -> processing -> completed with `writingAnalysis` populated.
</verification>

<success_criteria>

- `frontend/src/pages/WritingTestPage.jsx` present and routable
- `POST /api/sessions/:sessionId/writing` accepts and persists submissions
- `ai-services/nlp-service` responds to `/analyze` with expected schema
  </success_criteria>

<output>
After completion, create `.planning/phases/06-writing-test/06-01-SUMMARY.md` describing any deviations.
</output>
