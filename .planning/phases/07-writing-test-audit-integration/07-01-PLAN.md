---
phase: 07-writing-test-audit-integration
plan: 01
subsystem: technical-writing-assessment
tags:
  - writing-test
  - nlp-evaluation
  - WritingTestPage
  - analysisService
requires:
  - 06-database-refinement-result-aggregation
provides:
  - technical-writing-assessment
  - nlp-writing-evaluation
  - writing-ui-refinement
affects:
  - backend/services/analysisService.js
  - backend/controllers/writingController.js
  - frontend/src/pages/WritingTestPage.jsx
tech-stack:
  added: []
  patterns:
    - NLP evaluation of written technical responses
    - Role-tailored technical writing task prompts
    - Light Theme UI with character limits & countdown progress
requirements:
  - REQ-writing-assessment
---

<objective>
Audit, fix, and integrate the Technical Writing Assessment module. Ensure written candidate responses are sent to `nlp-service` for technical relevance, structural clarity, grammar, and completeness evaluation, and render a refined Light Theme UI in `WritingTestPage.jsx`.
</objective>

<execution_context>
- Candidate written responses are submitted via `POST /api/sessions/:sessionId/writing`.
- `analysisService.js` routes written submissions to `nlp-service` at `http://127.0.0.1:8003/analyze`.
- Results are persisted to `session.writingSubmission` and `session.writingAnalysis` in MongoDB.
</execution_context>

<tasks>
<task id="1" type="auto">
  <name>Backend — Writing Assessment NLP Integration</name>
  <files>
    - backend/services/analysisService.js
    - backend/controllers/writingController.js
  </files>
  <action>
    1. Update `sendTextToAnalyzer` in `backend/services/analysisService.js`:
       - Forward `question` (writing task prompt), `text` (candidate written answer), and `questionType: "technical"` to `nlp-service`.
       - Save returned multidimensional evaluation object in `session.writingAnalysis`.
    2. Update `backend/controllers/writingController.js`:
       - Attach role-tailored default writing task prompt if `session.writingTask` is empty.
  </action>
  <acceptance_criteria>
    - `session.writingAnalysis` is populated by `nlp-service` with structured scores and feedback.
    - Handled cleanly in background analysis queue.
  </acceptance_criteria>
</task>

<task id="2" type="auto">
  <name>Frontend — WritingTestPage Light Theme & Task Prompt UI</name>
  <files>
    - frontend/src/pages/WritingTestPage.jsx
  </files>
  <action>
    1. Upgrade `frontend/src/pages/WritingTestPage.jsx`:
       - Apply clean Light Theme styling (`#F6F5F0` background, rich off-white card).
       - Display writing prompt card showing role-specific technical question (e.g. *"Design a fault-tolerant rate limiter service"*).
       - Add live character count indicator, countdown progress bar, and clean submit workflow.
  </action>
  <acceptance_criteria>
    - `WritingTestPage.jsx` renders writing prompt, live character count, and time countdown.
    - `npm run build` passes with 0 errors.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- `npm run build` in `frontend` passes with 0 errors.
- Submitting writing test triggers analysis and populates `session.writingAnalysis`.
</verification>

<success_criteria>
- Technical writing module fully operational and evaluated by `nlp-service`.
</success_criteria>
